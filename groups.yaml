
all_windows:
  name: All Windows
  entities:
    - binary_sensor.dining_l
    - binary_sensor.dining_m
    - binary_sensor.dining_r
    - binary_sensor.fireplace_l
    - binary_sensor.fireplace_r
    - binary_sensor.kitchen_l
    - binary_sensor.kitchen_m
    - binary_sensor.kitchen_r

all_doors:
  name: All Doors
  entities:
    - binary_sensor.shed_door
    - binary_sensor.back_door
    - binary_sensor.front_door
    - binary_sensor.r_garage_sensor
    - binary_sensor.k_garage_sensor

dining_window:
  name: Dining Window
  entities:
    - binary_sensor.dining_l
    - binary_sensor.dining_m
    - binary_sensor.dining_r

lining_room_window:
  name: Living Roow Window
  entities:
    - binary_sensor.fireplace_l
    - binary_sensor.fireplace_r

kitchen_window:
  name: Kitchen Window
  entities:
    - binary_sensor.kitchen_l
    - binary_sensor.kitchen_m
    - binary_sensor.kitchen_r

garage_doors:
  name: Garage Doors
  entities:
    - binary_sensor.r_garage_sensor
    - binary_sensor.k_garage_sensor